export type UserFormData = {
  username?: string;
  email?: string;
  password?: string;
  confirmPassword?: string;
  photoURL?: string;
};

export type buttonType = {
  variant: "text" | "contained";
};

